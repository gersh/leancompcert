# Proposition 12.2.4 shared SPF table

Date: 2026-08-14

This is the complete compiled smallest-prime-factor table used by the runtime
coprimality checker.  It covers every `r` needed by the paper-faithful cell
range, not a sampled prefix.

Configuration:

- requested rows: `23,278,719` (`r = 1 ... 23,278,719`);
- segment length: `8,192`;
- segments: `2,842` (padded endpoint `23,281,664`);
- ascending prime-table entries: `649`;
- mark steps per segment: `21,302`;
- exact rolled loop count: `83,819,003`;
- array length: `23,298,702` words.

The positive source was generated by
`bench/Prop1224SPFTableEmit.lean`.  Its SHA-256 is
`304e3b129605021a113336f5763feef3edebe3e608e63e90be265feb61288332`
and its size is 60,051 bytes.  CompCert `ccomp -O` took 0.14 wall-s and
34,816 KiB peak RSS.  The complete run returned `violations 0` and stored
zero in the retained result cell in 1.15 user-s / 1.21 wall-s, at 183,200 KiB
peak RSS.

The fail-safe `ArrayAudit` source has SHA-256
`c136054e7ae3a5760dd3c55d3336f50b9a6422d71297f280f36f2b5762b8c6d2`
and size 162,496 bytes.  CompCert took 0.29 wall-s and 62,656 KiB peak RSS.
The complete audit returned `audit 0` in 1.35 user-s / 1.42 wall-s, at
183,204 KiB peak RSS.

All emission, compilation, and execution commands ran in user-systemd scopes
with `MemoryMax=64G`, `MemorySwapMax=0`; CompCert also had a 64 MiB stack
limit.  A 1,000-row smoke artifact was checked against an independent
trial-division smallest-factor oracle for its first 256 published cells.

`Prop1224SPFTable.program_compiled` has the fresh trust print
`[propext, Quot.sound]`.
